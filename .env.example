DATABASE_URL="file:./dev.db"

# Comma-separated absolute paths to local folders the planner should index/link
# (e.g. your university notes, ARUS repos, projects). Leave empty to disable.
WATCHED_FOLDERS=""

# Phase 2 (Google integrations) — leave empty until you set them up
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI=""
