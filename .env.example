DATABASE_URL="file:./dev.db"

# Comma-separated absolute paths to local folders the planner should index/link
# (e.g. your university notes, ARUS repos, projects). Leave empty to disable.
WATCHED_FOLDERS=""

# Google Calendar/Drive/Gmail credentials (Fase 2) are entered from the app
# itself at /ajustes and stored in the local database — not here. See the
# README ("Fase 2") for how to get them from Google Cloud Console.
